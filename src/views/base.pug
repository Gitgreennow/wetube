doctype html 
html(lang="ko")
    head    
        title #{pageTitle} | #{siteName}
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header  
            nav 
                ul 
                    li  
                        a(href="/") Home
                    li  
                        a(href="/search") Search
                    if loggedIn 
                        li  
                            a(href="/videos/upload") Upload Video
                        li 
                            a(href=`/user/${loggedInUser._id}`) #{loggedInUser.name}의 Profile   
                        li
                            a(href="/user/edit") Edit Profile 
                            
                        li 
                            a(href="/user/logout") LogOut
                        
                    else
                        li
                            a(href="/login")  Login  
                        li
                            a(href="/join")  Join
            h1=pageTitle
        main     
            block content
    include partials/footer